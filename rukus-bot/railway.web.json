{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install --frozen-lockfile=false && pnpm --filter @rukus/db exec prisma generate && pnpm --filter @rukus/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @rukus/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
